/** @format */

import {AppRegistry} from 'react-native';
import MapIntegration from './app/screens/MapScreen';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => MapIntegration);

// import {AppRegistry} from 'react-native';
// import App from './App';
// import {name as appName} from './app.json';
//
// AppRegistry.registerComponent(appName, () => App);
